# AI Handover — mychef.id

**Start here:** `AI_INSTRUCTIONS.md` then `AI_MAP.md`.

Do not use older handover text in this file. Sprint state belongs in git history and PRs, not a second instruction set.
