# KNOWN ISSUES

_Open items found during the 2026-06-23 control audit. Status: OPEN unless noted. Do not claim fixed without evidence._

## Structural
1. **Three nested git repos.** Root (deploys mychef.id) + `app/` + `Mychef Live/`. _Audited 2026-06-23:_
   - `app/` = **stale duplicate** of the marketing site, does not deploy, **106 uncommitted files** (1.2 GB). Risk: edits land in a dead tree. _Guards:_ documented in `CLAUDE.md`/`DO_NOT_TOUCH.md`/`PROJECT_CONTROL_STATUS.md` + physical marker `app/_DO_NOT_USE_STALE_DUPLICATE.md`. _Pending owner:_ back up the 106 uncommitted files, then archive/remove.
   - `Mychef Live/` = **a SEPARATE Next.js application** (auth/admin/chef-application platform, 1.3 GB, 0 uncommitted) — NOT a duplicate. Do not delete; confirm with owner whether it is active. Not part of the marketing-site repo's concerns.
2. **Vestigial `/blog` surface.** `/blog` 301→`/journal`, yet `BlogIndexPage.tsx` + a `/blog` entry in `scripts/prerender.mjs` + `scripts/inject-meta.ts` still exist (prerenders an unreachable page). Low risk; clean up as a deliberate decision.
2b. **Unrelated uncommitted working-tree edits (noted 2026-06-23, NOT touched by the route sprint).** Left in the working tree, owner to review: `app/` (the stale duplicate — gitlink/106 files), `public/_redirects`, and `src/data/redirects.tsx`. These were deliberately **not committed** (route sprint committed only `vercel.json`, `scripts/prerender.ts`, `public/sitemap.xml`, `src/data/sitemap.ts`). Review `public/_redirects` + `src/data/redirects.tsx` before any future commit — see NEXT_ACTIONS repo-hygiene priority.
2c. **Git worktree friction.** Sandbox cannot commit (worktree gitdir lives outside the mount; recurring stale `.git/index.lock`). Autonomous commits this sprint required running git via the owner's Mac shell (`osascript do shell script`) plus a stale-lock removal. A harmless `git diff` pager (from a manual review command) may still be paused in Terminal — safe to quit (`q`) or ignore. Fixing this friction is a recommended next-sprint priority.

## Tracking (Skill 06) — re-audited 2026-06-23
3. **CORRECTED — index.html is GTM-only.** Earlier note ("hardcoded GA4 gtag in index.html") was wrong: `G-W0PQH8ZKTF` in `index.html` is only a *comment*. GTM (`GTM-KCBNZBL9`) is the sole loader; GA4 is configured inside GTM. `index.html` NOT changed.
   - **page_view: SINGLE (OK).** `trackPageView` has zero callers (manual SPA page_view removed in `0d507c8`); page views come from GA4 via GTM + Enhanced Measurement only.
   - **Conversion `generate_lead`: FIXED (Vector 1).** `Layout.tsx` global delegated listener fires on every `wa.me`/`tel:` click, AND 5 components (ServicePage, PillarSubPage, StickyMobileCTA, ExitIntentPopup, WhatsAppButton) also fired `trackWhatsAppClick` on the same anchors → double `generate_lead`. Removed the redundant component calls; preserved attribution via `data-source`. Global listener is now the single source. ConciergeWidget kept (uses `window.open`, not caught by the listener). Verified `tsc -b` exit 0.
   - **Vector 2 (gtag + dataLayer dual-send) — RESOLVED via live runtime test 2026-06-23.** Measured on mychef.id in-browser: `window.gtag` is **`undefined`** in page scope, so `trackEvent`'s `window.gtag?.('event',…)` is an inert no-op. GA4 receives events ONLY through `dataLayer` → GTM. One WhatsApp click = exactly **one** `generate_lead` dataLayer push. **No double-send from the gtag path; no code change made** (per the rule "remove gtag only if duplication is proven" — it is not). The dead `window.gtag?.()` line is harmless; remove only if a future DebugView shows duplication.
   - **VERIFIED DIRECTLY IN GTM + GA4 ACCOUNTS (2026-06-23) — double-count question CLOSED.** Container `GTM-KCBNZBL9` (account 6362130619 / container 256177628, Live v2): exactly **2 tags** (GA4 Config All-Pages; GA4 Event `whatsapp_click`) + **1 trigger** (Link Click, "Click URL contains wa.me"). Tag Assistant Preview, one WhatsApp click → GA4 Config ×1, `whatsapp_click` ×1, **no other tags** → **no double-fire**. GA4 Enhanced-Measurement outbound `click` is **NOT** a key event → no double conversion. page_view = single GA4 config.
   - **Real issue found + FIXED (owner-approved): WhatsApp conversion was not counting.** GTM sends `whatsapp_click`; a GA4 event-modification "Map WhatsApp Click to Conversion" was renaming it to `whatsapp_contact`, which was **not** a key event → 0 conversions counted. Per owner decision, changed the modification's New Value `whatsapp_contact` → **`generate_lead`** (the existing key event). Now: WhatsApp click → `whatsapp_click` → renamed to `generate_lead` (key event) → **one conversion**. Change is in GA4 (reversible), not in code/index.html/GTM.
   - **Consent + collection audit (2026-06-23) — GA4 IS collecting; 0-Realtime was a test artifact.** `google_tag_data.ics` shows `analytics_storage`, `ad_storage`, `ad_user_data`, `ad_personalization` all **implicit (unset) → effectively granted**; **no Consent Mode default, no CMP/cookie banner, no denial**. GA4 7-day data is healthy (page_view 207, whatsapp_contact 208, generate_lead 2; users from Indonesia, US, Canada, etc.). This browser does NOT block GA (`/collect` OK). A GA4 **"Internal Traffic" data filter (Exclude, state: Testing)** exists → developer/owner traffic is tagged internal. **Conclusion:** the 0 active users in my Realtime test = internal-traffic tagging + automated/synthetic session + Realtime latency, NOT consent/blocking/collection failure. **No cookie banner needed for Indonesia / non-restricted regions.**
   - **Compliance note (recommendation, not a bug):** with no Consent Mode/CMP, EEA/UK/Switzerland visitors are currently tracked with cookies *without* consent (GDPR/UK PECR/Swiss FADP gap). If meaningful EEA/UK/CH traffic exists, add a **region-aware** Consent Mode v2 + minimal CMP that defaults analytics/ad storage = denied for those regions only and granted elsewhere (incl. Indonesia). Do NOT apply denied consent globally. See NEXT_ACTIONS.
   - **Phone tracking added 2026-06-23 (GTM Live v3 + GA4 modification).** "Phone Click" trigger (`tel:`) + "GA4 Event - phone_click" tag + modification `phone_click → generate_lead`. Both WhatsApp and phone now map to the single `generate_lead` conversion.
   - **Still open (owner):** confirm `generate_lead` fires once from a real (non-internal) device for BOTH a WhatsApp and a phone click; decide whether to set the Internal Traffic filter to Active.

## SEO / schema (verify)
4. **Sitemap vs redirects integrity.** 151 sitemap URLs; must confirm none are redirect sources or 404s (Skill 05). Two prior reverts touched sitemap/links — re-verify.
5. **Two cost-guide pages** (`/blog/private-chef-cost-bali` and `/blog/private-chef-bali-cost-breakdown-detailed-2026`). Confirm they don't compete/duplicate; pick a canonical if they overlap.
6. **Global vs per-page rating reconciliation** — resolved by removing all rating markup (`21390a7`). If reviews are ever added back, must be real + on-page (Skill 08).

## Internal linking
8. **`/private-chef-bali` appears underlinked** — only ~1 inbound internal link found in `src/` (vs `/pricing` 47, `/chefs` 26). It is a primary money keyword ("private chef bali"). Verify true inbound count (nav may link via a variable) and, if low, add contextual links from hubs (Skill 09). _Sitemap integrity verified clean: 0 of 151 sitemap URLs are redirect sources._

## SEO — sitemap/indexability (audited 2026-06-23)
9. **Sitemap itself is clean.** All 151 `<loc>` return **200**, all have a **self-referential canonical**, 0 redirected/4xx/5xx, 0 robots-blocked (only `/404` is Disallowed and not in sitemap), 0 soft-404s. No live page is missing from the sitemap.
10. **RESOLVED 2026-06-23 (commits `0c25cbd` + `cdb7bef`, live + verified) — the six internally-linked direct-access routes are fixed.**
    - `/villa-chef`, `/recommended-services`, `/join-our-team` → now **HTTP 200** on direct access, real titles, self-referential canonicals (not soft-404s). Footer → `/join-our-team` now lands on 200.
    - `/terms-of-service`, `/privacy-policy`, `/payment-terms` → now **308 permanent redirect** to `/terms`, `/privacy`, `/cancellation` (targets are 200).
    - **Root cause (system learning):** the static route files served on Vercel are generated by `scripts/inject-meta.ts` from **`src/data/sitemap.ts` (`SITEMAP`)**, NOT by `scripts/prerender.ts` (which is skipped on Vercel — no Chromium). The three content routes were absent from `SITEMAP`; adding them (4-line edit, `cdb7bef`) made inject-meta emit `dist/<route>/index.html`. The legacy aliases were fixed by 301/308 redirects in `vercel.json` (`0c25cbd`). See Skill 05 "CRITICAL" section. _The earlier "~32 routes 404" finding: the other ~26 non-prerendered routes are not internally linked and were out of scope; re-check separately if needed._

## Process
7. **History shows repeated rework** (add→revert pairs, USD→IDR sweeps across many commits, TS build breakages from untracked files). The skills system + agents created this sprint are the mitigation; enforce them.
