# AI Skills Setup — mychef.id

Every agent reads `AI_INSTRUCTIONS.md`, then `AI_MAP.md`, then `/ai-skills/`.

`npm run verify:ai-skills` and `.github/workflows/ai-skills-check.yml` enforce that those files exist. There is no second skill folder.
