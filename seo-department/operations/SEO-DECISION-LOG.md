# SEO Decision Log — myCHEF.id

Every material decision is recorded here: what was decided, on what evidence, by whom, and what happened. Conflicts between sources are logged rather than silently resolved.

| ID | Date | Decision | Rationale / evidence | Alternatives considered | Decided by | Reversible? | Outcome / review date |
|---|---|---|---|---|---|---|---|
| D-001 | 2026-07-28 | Establish AI SEO department under `seo-department/` | Client brief; needed persistent structure across sessions | Ad-hoc per-session working | Management | Yes | Review 2026-08-28 |
| D-002 | 2026-07-28 | Do NOT classify the 61 area pages as doorway pages | Inspected `privateChefAreas.ts`: unique intro, villa density, guest profile, landmarks, coordinates, FAQs per area (3,236 lines). Fails the doorway definition | Recommend mass consolidation | 01 (verified) | Yes | Revisit after GSC cannibalisation data |
| D-003 | 2026-07-28 | Block all baseline reporting until data access granted | Cannot report performance without GSC/GA4/GBP. Reporting estimates as results would breach the department's own reporting rule | Estimate a baseline from third-party tools | 01 / 18 | Yes | On access grant |
| D-004 | 2026-07-28 | Consolidation-and-measurement before new content | 248 URLs already live with unmeasured performance; adding volume compounds an unmeasured problem | Begin publishing new pages immediately | 01 / 09 | Yes | Review at day 30 |
| D-005 | 2026-07-28 | Escalate service-area coverage as a business question, not an SEO one | Whether myCHEF serves Munduk or Nusa Penida is an operations fact the department cannot invent | Quietly unpublish remote areas | 01 / 24 | Yes | Awaiting management |
| D-006 | 2026-07-29 | Harden `scripts/publish-content.py` against silent data loss | Dry run proved the parser failed on the current `articleContent.ts` (`};` terminator) and a real run would have written 21 of 161 entries — deleting 140 article bodies. Added tolerant regex, fail-fast on parse failure, and a guard refusing to write fewer entries than exist | Leave script as-is and rely on git recovery | 06 / 23 | Yes | Verified by dry run 2026-07-29: 161→161, zero drops |
| D-007 | 2026-07-29 | Backport all 2026-07-28 shipped content fixes to `mychef-seo/content/` markdown | 13 upstream files still carried pre-fix wording (50% deposit claims, "8+ years" vs "since 2019", tax phrasing, salary quoting, 2 `[BUSINESS CONFIRMATION REQUIRED]` markers); next publish run would have reverted 12 live routes | Freeze the publish script instead | 10 / 11 | Yes | Dry run divergence: NONE |
| D-008 | 2026-07-29 | General guest minimum is 5 for dining formats (catering, fine dining, three-course/family style, grazing, retreat, plated) | Owner decision in commit c825330f, scope re-confirmed by owner 2026-07-29: applies to dining formats, NOT events (own numbers) and NOT kids parties (6 children stays). Babi guling 6 (whole-pig) and buffet 30 retained as operational. Chef's Table "seats six" is capacity, untouched. 39 replacements across 22 files (site + upstream md + articleContent) | Catering-only scope; site-wide including kids | Management (owner) | Yes | Residual 6-references audited: all intentional |
| D-009 | 2026-07-29 | Remove stale IDR 5M minimum-spend claim from plated FAQ (site + upstream md) | c825330f abolished the 5M minimum spend but the FAQ on `/catering/plated-catering` still cited it — a live price-claim contradiction | Leave until next content pass | 11 / 24 | Yes | Fixed both sources 2026-07-29 |

| D-010 | 2026-07-29 | Consolidate "private chef {area}" intent onto `/private-chef/{area}`; re-point 10 legacy redirects | GSC export (analysis_961, 28 Apr–26 Jul): legacy short URLs (`/canggu` pos 4 "private chef canggu", pos 12 "private chef bali") were 301ing to `/locations/{area}` dining guides, which now own the service-intent rankings (`/locations/canggu` pos 5, 288 imp) while `/private-chef/{area}` pages are nearly invisible (3 imp). Owner chose consolidation over strengthening the guides | Strengthen guides; wait for more GSC data | Management (owner) | Yes — redirects reversible | Watch "private chef canggu" ranking over 4–6 weeks; expect short-term turbulence |
| D-011 | 2026-07-29 | Target the unowned "private chef villa menu" query family (640 imp, 21 URLs pos 24–98, zero clicks) with `/fine-dining/menus` | Largest unowned query family in the export; page already titled for it but H1 was "Our Menus" and no internal anchors used the phrase. H1/title/description sharpened + anchors added on Canggu/Seminyak/Ubud area pages | New dedicated page (rejected: cannibalisation risk) | 08 / 14 | Yes | Review impressions for the family after next GSC pull |

| D-012 | 2026-07-30 | Expand and correct `/catering/babi-guling` for search-intent depth, but **exclude** wedding, private-chef and villa-catering keyword groups from the supplied target list | Owner requested "all keywords" coverage. GSC export (`data/gsc-2026-07-28`) shows those groups are already earning on other URLs: `bali wedding catering` 424 imp (pos 27.9), `private chef bali` 749 imp (pos 18.4), `bali villa catering` 94 imp (pos 15.4). Targeting them here would deepen R-002 / C-004 cannibalisation against 1,270+ impressions of existing equity. Ownership map assigns this URL `Babi Guling catering Bali` only | Target the full supplied list as requested (rejected on cannibalisation evidence); surgical metadata-only fix (rejected — page genuinely lacked intent depth) | Management (owner), on 3-option choice | Yes | Review after next GSC pull: watch whether babi-guling page impressions rise without `/events/weddings` or `/private-chef/*` falling |
| D-013 | 2026-07-30 | Correct four verified metadata/schema contradictions on `/catering/babi-guling` | (a) meta description said "10-50 guests" against verified 6-guest minimum (D-008) and 100+/450+ capacity; (b) `page-meta.ts` h1 did not match the rendered H1, despite `page-meta.ts` being designated source of truth; (c) ogImage in `page-meta.ts` (`/hero-catering.webp`) differed from the one passed to `SeoHead`; (d) schema `lowPrice` was 550000 while `BabiGulingPricing` carries a 525000 tier for 51–100 guests | Leave until a wider metadata pass | 08 / 13 / 24 | Yes | Fixed 2026-07-30; FAQ array rewritten in the same pass so `faqPageSchema` stays consistent (per standing rule from D-008 fallout) |
| D-014 | 2026-07-30 | Capture "pork free catering Bali" demand with a dedicated section on `/catering/babi-guling`, and open a brief for a standalone page | GSC export shows `porkfree catering bali` (17 imp, pos 22.5), `pork free catering bali` (11 imp, pos 10.1), `porkfree katering bali` (10 imp, pos 23.3) — 38 impressions combined, **more than `/catering/babi-guling` itself earned (18)**, and no URL on the site targets the intent. Section added now; intent is distinct enough from whole-roast-pig to warrant its own URL | Ignore (rejected — measured, unserved demand); build standalone page immediately (deferred — needs D-004 consolidation discipline and a management call) | 04 / 09, owner-approved | Yes | Brief to be written; review at next GSC pull |
| D-015 | 2026-07-30 | Close CF-003 / H-02 as a documentation error rather than a pricing conflict; make no page change | Owner ruled the babi guling IDR 3,900,000 minimum correct and the hub figure wrong. Re-verification against source found there was no hub figure to correct: `CateringMainPage.tsx` carries no minimum-spend claim at all — its only "minimum" is "Minimum 5 guests" (line 432, repeated 1201) and its IDR 5,000,000 is a 25–30 guest package tier price (line 131). `CateringPlatedPage.tsx:598` states "there is no minimum spend", the opposite of what H-02 recorded. The only spend minimum on the site is `siteFacts.corporateMinSpend` IDR 15,000,000. Two further H-02 rows were also stale: BBQ is 10 guests (logged as 6), grazing is 5 (logged as 6) | Edit the hub to match the ruling (rejected — there was nothing to edit; the change would have introduced a minimum that does not exist); raise all pages to a 5,000,000 floor (rejected — no such rule) | Management (owner) ruling, verified against source | Yes — documentation only | Register now requires file:line citations. Retreat minimum (Q7) remains genuinely open and was re-filed out of the closed H-02 |
| D-016 | 2026-07-30 | Do not document per-area babi guling logistics; close CF-004 as will-not-do | Owner ruled open-flame restrictions, per-area travel bands and banjar function fees are only material for very large groups, so per-area detail is out of scope for a page whose stated range is 6–50 guests. The area section was already written around verified variables rather than invented per-area facts, so closing this required no edit | Supply real per-area data (declined by owner); research independently with confidence flags (rejected — owner ruled the topic out of scope, not merely unverified) | Management (owner) | Yes | The page retains general, non-area-specific mentions of on-site roasting feasibility and out-of-area travel cost. Flagged to owner for a separate call on whether those should also go |

| D-017 | 2026-07-30 | Do **not** convert the inline `provider: { '@type': 'Organization' }` on `/catering/plated-catering` to `providerRef`; treat only `LocalBusiness` providers lacking `address` as defects | Autopilot run 2 flagged plated-catering as a miss from the `650ba745` sweep. It is not. The defect that sweep fixed was `provider: { '@type': 'LocalBusiness' }` **without** `address` — `LocalBusiness` requires `address` in schema.org, `Organization` does not. This is why Semrush issue 45 listed exactly 3 URLs (`/wedding-catering-indonesia`, `/proposal-dinner`, `/experiences/kids-birthday-chef-party`) and not this one. Repo sweep confirms the four remaining inline `LocalBusiness` providers (`EventsBirthdaysPage.tsx:94`, `EventsVillaPartiesPage.tsx:162`, `EventsBabyShowersPage.tsx:32`, `VillaEventPackagesPage.tsx:80`) all already carry `address: postalAddressSchema` | Normalise every provider to `providerRef` for consistency (rejected — a valid `Organization` provider is not an error; the edit would be churn with no measurable gain, against a stale snapshot) | Autopilot run 2, verified against `650ba745` diff and schema.org required-property rules | Yes — no change made | Recorded to stop a future run "fixing" a non-defect. If Semrush ever flags plated-catering under issue 45, revisit |
| D-018 | 2026-07-30 | Close Q7: state the **five-guest minimum** on `/catering/retreat-catering` rather than "Minimum booking applies" | Owner instructed the open items be actioned. The retreat minimum was never genuinely unknown: **D-008** set a 5-guest minimum for dining formats and named retreat explicitly, and `WellnessRetreatCateringPage.tsx:148` already publishes "5 guests minimum for a dedicated retreat catering program". Meanwhile "Minimum booking applies" (`CateringRetreatPage.tsx:769`) implied a **spend** minimum, which **D-015** established does not exist outside corporate (IDR 15,000,000). The FAQ at `:455` asked "is there a minimum?" and never answered it. Both now state five guests | Ask the owner for a number (rejected — the number was already ruled in D-008 and already live on a sibling page; asking again would re-litigate a settled decision); leave the vague wording (rejected — it implies a non-existent spend minimum) | Management (owner) instruction, applied against D-008 + D-015 | Yes | No new fact invented. `FAQS` is a single array feeding both `faqPageSchema` (`:491`) and `FAQAccordion` (`:1168`), so schema and on-page copy stay consistent. If the owner's real retreat minimum differs from 5, correct D-008 first — this page follows it |

| D-019 | 2026-07-29 | Collapse the `/corporate-events-catering-bali` 301 chain to a single hop | `vercel.json` served two real edge 301s: `/corporate-events-catering-bali` → `/blog/corporate-events-catering-bali` (`redirects.ts:151`) → `/corporate-case-studies` (`redirects.ts:265`). The intermediate URL matches no route in `App.tsx`, no entry in `route-slugs.ts` and no `<loc>` in `sitemap.xml` — it exists only as a redirect source. A transitive scan of all 205 rules confirmed this was the only chain and there are no loops | Leave the chain (rejected — link equity and crawl budget are lost on a hop that resolves to nothing); delete the rule entirely (rejected — it has GA4-measured traffic, 4 views) | Autopilot run 5, verified against `vercel.json` + repo | Yes | Re-verified post-change: 0 chains, 0 loops across 205 rules |
| D-020 | 2026-07-29 | Restore D-010's ten `/private-chef/[area]` redirect targets in `src/data/redirects.ts`, the generator source | **D-010 was silently reverting on every deploy.** `edaeb854` implemented it by hand-editing `vercel.json` only — `git show --stat edaeb854` lists five files and `src/data/redirects.ts` is not among them. But `scripts/generate-redirects.ts:85` overwrites `vercel.json` from `redirects.ts`, and `package.json` wires that into `prebuild`, which `vercel build` runs on every CI deploy (`.github/workflows/deploy.yml:64`). Pre-fix state: `redirects.ts` and `public/_redirects` said `/locations/*` for exactly the ten URLs `edaeb854` changed, while `vercel.json` said `/private-chef/*` — ten mismatches, no others. All ten new targets verified live (`App.tsx:393` generates `/private-chef/[slug]`; each is in `sitemap.xml`) | Re-edit `vercel.json` again and leave the source stale (rejected — the next `prebuild` reverts it, which is how this arose); extend the ruling to the other nine `/locations/` redirects (rejected — D-010 named ten URLs; widening it would invent scope) | Autopilot run 5, applying owner ruling D-010 | Yes | Scope held to D-010's ten URLs. `/jimbaran`, `/pererenan`, `/bukit`, `/legian`, `/kerobokan`, `/petitenget`, `/tanah-lot`, `/denpasar`, `/gianyar` deliberately still point at `/locations/`. Watch "private chef canggu/seminyak/ubud" — D-010's 4–6 week review window effectively restarts now, since the ruling was not actually live |
| D-021 | 2026-07-29 | Do NOT act on Semrush issue 213 ("pages with only one internal link", 4 blog URLs) | The audit crawls 100 pages against 248 sitemap URLs (`pages_crawled: 100, pages_limit: 100`), so link-count metrics are measured inside a 40% keyhole. Each of the four URLs was checked against the repo and has 2–4 genuine inbound internal links: `InVillaServicePage.tsx:392`, `EventsMainPage.tsx:1043`/`:1058`, `HubPage.tsx:1385`, `PrivateChefDenpasarGuidePage.tsx:19`/`:112` | Add internal links to satisfy the tool (rejected — would be padding links to fix a measurement artifact, not a real orphan) | Autopilot run 5, verified against repo | Yes — no change made | Recorded so a future run does not "fix" a non-defect. Revisit only if the crawl limit is raised and the issue persists |


| D-022 | 2026-07-29 | Fix `/calculator` `X-Robots-Tag` at the generator source (`scripts/generate-redirects.ts:80`), from `noindex, nofollow` to `noindex,follow` | Same failure mode as D-020. `fe16f93d` ("close /calculator robots conflict") changed the value in the generated `vercel.json` only; `git show --name-only fe16f93d` confirms `scripts/generate-redirects.ts` was untouched, so every `prebuild` reverted it — observed directly when the other session's build regenerated `vercel.json` in the working tree. Compounding it, `scripts/inject-meta.ts:376` lists `/calculator` in `noindexFollowPaths`, so the meta tag said `noindex,follow` while the header said `noindex, nofollow` — the page contradicted itself and the stricter header won, blocking PageRank flow through a page carrying the full site nav | Re-edit `vercel.json` again (rejected — that is the bug); leave it (rejected — a logged, shipped decision was being reverted on every deploy) | Autopilot run 5, verified against `fe16f93d` diff + `inject-meta.ts:376` | Yes | `npx tsc -b` exit 0. Also establishes the standing rule: never edit `vercel.json`, `public/_redirects` or `public/sitemap.xml` by hand — they are build outputs |


## Open conflicts requiring resolution
| ID | Conflict | Sources | Status |
|---|---|---|---|
| CF-001 | Pricing appears across `/pricing`, `/blog/private-chef-cost-bali`, `/journal/bali-private-chef-cost-guide-2026`, `/michelin-private-chef-bali-prices` and per-area `priceFrom` values | Four+ site locations | **Unresolved — requires single approved price source from management** |
| CF-002 | "Michelin-trained" and "560+ villas" claims used in metadata | `page-meta.ts` vs no evidence document on file | **Unresolved — evidence required** |
| CF-003 | Babi guling minimum total is stated as IDR 3,900,000 (6 × 650,000) on `/catering/babi-guling`, while the catering hub was believed to carry a IDR 5,000,000 minimum spend | `/catering/babi-guling` vs `/catering` hub; was H-02 in `catering-cluster/CONTRADICTION-REGISTER.md` | **RESOLVED 2026-07-30 (D-015) — the conflict did not exist. The hub carries no minimum-spend claim; its IDR 5,000,000 is a 25–30 guest package tier price (`CateringMainPage.tsx:131`). Owner ruled 3,900,000 correct. No page change required** |
| CF-004 | Per-area babi guling logistics (which areas commonly restrict open flame, per-area travel bands within the IDR 250K–700K range, typical banjar function fee) are undocumented | Area coverage section on `/catering/babi-guling` | **CLOSED 2026-07-30 (D-016) — owner ruled not to write about these. They only arise for very large groups, so per-area detail is out of scope for a page serving 6–50 guests. Will not be added** |
