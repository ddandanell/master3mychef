# myCHEF.id — SEO To-Do List

**From:** `SEO-FULL-AUDIT-2026-08-05.md` · **Created:** 2026-08-05

How to read this: work top to bottom. Each item says exactly which file to change and how you know it worked. `[ ]` = you or a developer does it. `[?]` = a decision only you can make.

**Standing rule:** never hand-edit `public/sitemap.xml`, `vercel.json` or `public/_redirects`. They are generated from source. Edit the source, rebuild.

> **Note:** an earlier version of this list told you to add `/private-chef-bali` to the sitemap. That was based on the stale committed `public/sitemap.xml`. The live sitemap already contains it. Step 3 below fixes the staleness that caused the error.

---

## ✅ ALREADY DONE — shipped 2026-08-05

Steps 1, 3, 4, 5, 9 (partial), 11 and 13 are complete. Logged as D-023 to D-029 in `SEO-DECISION-LOG.md`. Risk register updated. `npx tsc -b` and `eslint` both clean.

| # | What changed | Files |
|---|---|---|
| 1 | **Homepage price snippet corrected** — now states both models: *"Chef day rates from IDR 1,000,000++, catering from IDR 700K per guest"* (155 chars) | `page-meta.ts:31` |
| 8 | **"Michelin-trained" removed from all 12 meta descriptions** and from the highest-visibility on-page claims → "Milan-trained" or a concrete alternative. **560+ figures kept** per your ruling | `page-meta.ts`, `siteArchitecture.ts`, `PrivateChefBaliPage.tsx`, `WhyMychefPage.tsx`, `FineDiningChefsPage.tsx` — 28 replacements |
| 4 | **Nine unsubmitted pages added to the sitemap** — turned out to be 9, not 3 (see correction below) | `page-meta.ts` (+10 entries), `sitemap.ts` (+9) |
| 3 / 5 | **Build guard added** — `checkRouteSitemapCoverage()` fails the build if any static route is neither in the sitemap, noindexed, nor redirected. Verified: 204 routes, **0 orphans**. Plus a "generated files are not truth" section in `AI_INSTRUCTIONS.md` | `verify-all.ts`, `AI_INSTRUCTIONS.md` |
| 5 | **Pillar `lastmod` refreshed** 2026-06-24 → 2026-08-04 (the page was rewritten by `55fd6f0b`) | `sitemap-lastmod.ts` |
| 9 | **URL collision disambiguated** — prominent link added from `/fine-dining/private-chef-bali` → `/private-chef-bali`, per your link-only choice | `PrivateChefBaliPage.tsx` |
| 11 | **Three sitewide footer links repointed** — "Tasting Menu at Your Villa" → the real subpage, "Reviews" → `/reviews`, "Private Chef Breakfast" → `/catering/floating-breakfast` | `Footer.tsx` |
| 2 / 13 | **Ten descriptions rewritten** — the position-6 food-safety snippet retargeted, plus the nine longest on high-impression pages. Over-160 count 65 → 52 | `page-meta.ts` |

**Two of my own findings were wrong and are corrected below:**

- **Step 1 was half-wrong.** IDR 700K/guest is *not* a discontinued price — `scripts/check-price-floor.ts` enforces `FLOOR = 700_000` as the live per-person catering floor on every build. The real defect was narrower: a homepage framed around "Private Chef Bali" quoted only the catering floor and omitted the chef day rate. Both are now stated. (D-028)
- **Step 4 was wrong about `/quote`.** It is deliberately `noindex,follow` (`inject-meta.ts:376`). Adding it to the sitemap would have recreated exactly the contradiction D-022 fixed for `/calculator`. It has been added to `NOINDEX_PATHS` instead. (D-027)

**And the sitemap gap was bigger than reported.** The guard found **eight** live, indexable, internally-linked routes that had never been submitted — not three. Seven are substantial blog pages (199–227 lines each, all with proper metadata) plus `/pricing-calculator` (587 lines, FAQ schema):

`/blog/bachelor-party-bali-private-chef` · `/blog/corporate-catering-bali-case-studies` · `/blog/dry-season-menu-bali` · `/blog/festive-season-menu-bali` · `/blog/private-chef-seminyak-canggu-ubud-comparison` · `/blog/private-dinner-party-bali` · `/blog/wet-season-menu-bali` · `/pricing-calculator`

Plus `/partner` (the villa-manager referral page — the exact audience step 18 targets first) and `/fine-dining/our-chefs` (in the sitemap but with no managed metadata).

**One new risk was opened:** `FoodAllergiesPage.tsx:242` states *"All myCHEF chefs are certified"* (HACCP). That is a food-safety credential claim on an allergy page — filed as **R-014**, needs your confirmation. See step 23.

**Still open for you:** steps 6, 7, 10, 12, 14–22 below, plus the residual Michelin scope in step 8 (139 body-copy occurrences across 47 files, and the two pages whose identity is the claim).

---

## WEEK 1 — quick, safe, high value (~2 hours total)

### 1. ~~Fix the homepage price in the meta description~~ ✅ DONE — and the original instruction was wrong

**This step as first written told you to delete a real, live, build-enforced price. Do not re-apply it.**

The original text claimed *"the IDR 700K/guest model no longer exists"*. That was my error. `scripts/check-price-floor.ts:26` sets `const FLOOR = 700_000` and enforces it against the built HTML on **every deploy** — IDR 700K per guest is the live catering floor, not a dead number. The site sells two models side by side:

| Model | Price | Source of truth |
|---|---|---|
| Private chef, per **day** | IDR 1,000,000 / 1,800,000 / 2,700,000 ++ | `siteFacts.ts` `MEAL_PLANS` (`daily:` fields) |
| Catering, per **guest** | from IDR 700,000 | `check-price-floor.ts` `FLOOR`, enforced at build |

The real defect was narrower: a homepage framed around *"Private Chef Bali"* quoted only the catering floor and omitted the chef day rate. Both are now stated.

**Shipped (`page-meta.ts:31`, 155 chars):**

> Private chef Bali for villa dining, BBQs, weddings & events. Chef day rates from IDR 1,000,000++, catering from IDR 700K per guest. WhatsApp within 1 hour.

Live and verified on `mychef.id`. Logged as **D-028**.

**The second half of the step — the street food page — is now resolved: it was already correct.**

`page-meta.ts:2564` advertises *"From IDR 700K/person ++"* for `/blog/indonesian-street-food-private-chef-bali`. Checked against the page's own content (`IndonesianStreetFoodBaliPage.tsx`): all three menus — Balinese Village Feast, Indonesian Archipelago, Street Food Night — are priced at exactly **IDR 700K/person**. The meta matches the page, and 700K sits exactly on the enforced floor. The only other figure, IDR 200K/person, is the dawn market-visit **supplement**, which `check-price-floor.ts` correctly treats as an add-on. **No change required.**

### 2. Fix the position-6 page that earns zero clicks — cheapest win in the report

- [ ] Query: **"how to ensure food safety and sourcing with a private villa chef in bali"** — 78 impressions, **position 6.0**, **0 clicks**
- [ ] Find which URL ranks for it (GSC → Performance → filter by that query → Pages)
- [ ] Rewrite that page's title and meta description to answer the question directly
- [ ] Add a short, clearly-formatted answer near the top of the page

**Done when:** the page's title and description visibly answer the question a searcher typed.
**Why:** ranking sixth and earning nothing means your snippet isn't answering the query (or an AI Overview is taking the click). Fixing a snippet is far cheaper than fixing a ranking.

---

### 3. Stop the sitemap file from going stale in git

- [ ] `public/sitemap.xml` was last committed **2026-07-30** (`d8ade71b`) but HEAD is **2026-08-04**. It is tracked in git and 5 days out of date
- [ ] Production is fine — `prebuild` regenerates it on every deploy. The problem is that the repo copy lies
- [ ] Choose one:
  - [ ] **Preferred** — add `public/sitemap.xml` to `.gitignore`. Check whether `vercel.json` and `public/_redirects` should follow (careful: Vercel may need `vercel.json` committed — verify before removing that one)
  - [ ] **Or** — add sitemap regeneration to the pre-commit hook so the committed copy can never drift
- [ ] Add to `AI_INSTRUCTIONS.md`: *"`public/sitemap.xml` may be stale. Verify against `https://mychef.id/sitemap.xml` or regenerate before reasoning about it."*

**Done when:** either the file is no longer in git, or committing a content change updates it automatically.
**Why:** this exact staleness produced three false "critical" findings in the first draft of this audit. It has now cost real analysis time twice (see also D-020, D-022).

---

### 4. Add the three genuinely missing pages to the sitemap

- [ ] In `src/data/sitemap.ts`, add to `infoPages` using the `metaInfo(...)` helper (they need `type`, `title`, `description` — copy the pattern of the surrounding entries):
  - `/quote` — **a conversion page**, and external backlinks already point at it
  - `/partner`
  - `/pricing-calculator`
- [ ] Add a `page-meta.ts` entry for **`/fine-dining/our-chefs`** — it is already in the live sitemap but has no managed title/description and is inheriting a fallback

**Do NOT add these** (verified, they are correct as-is):
- ✗ `/about` — it is a 301 to `/chefs` (`redirects.ts:149`). Adding a redirect source to the sitemap is a defect
- ✗ `/restaurant-kitchen-solutions` and the six `/guides/*` pages — **already in the live sitemap**
- ✗ `/calculator`, `/join-our-team` — deliberately in `NOINDEX_PATHS`
- ✗ The 22 `/bar-services/` URLs — deliberately excluded by owner decision, 2026-07-28

**Done when:** the live sitemap contains `/quote`, `/partner`, `/pricing-calculator` after the next deploy.

---

### 5. Refresh the pillar page's lastmod

- [ ] `/private-chef-bali` shows `lastmod 2026-06-24` in the live sitemap
- [ ] But the page was **materially rewritten on 2026-08-04** — new pricing model and the Chef Rotation feature
- [ ] Update its date in `src/data/sitemap-lastmod.ts` (or wherever its date resolves from) to 2026-08-04

**Why:** you did significant work on your most important commercial page and the freshness signal doesn't reflect it.

---

## WEEK 2–3 — decisions and structure

### 6. [?] DECIDE: what to do about the satellite sites — HIGHEST RISK ITEM

Two sites link to mychef.id and appear to be under your control:

- **`villa-catering-bali.online`** — 11+ links. Every linking page links to **nothing on the internet except mychef.id** (`external_num: 1`). Anchors are your own CTAs: *"Book a chef now →"*, *"Browse luxury chefs →"*, *"Compare prices →"*, *"See wedding-ready chefs →"*, *"Start a dietary-aware quote →"*
- **`balipoolmaintenance.online`** — 9 links, exact-match anchor *"hire a private chef in bali"*

- [?] **Are these yours?** → `________________`
- [?] If yes, choose:
  - [ ] **A** — keep them as genuinely independent businesses with their own audiences, and `rel="nofollow"` every link to mychef.id
  - [ ] **B** — fold the useful content into mychef.id and 301 the domains
  - [ ] **C** — leave as-is *(I'd argue against this — it is the exact footprint Google's link-spam systems look for)*

**Why:** a network of sites existing mainly to pass links to one commercial domain is a link scheme under Google's policies, regardless of content quality. Highest-risk finding in the audit.

---

### 7. [?] DECIDE: were the three media links paid?

- **tribunnews.com** (Authority 80) — `bali.tribunnews.com/**adv**/602248/…` — the `/adv/` path suggests advertorial
- **voi.id** (58) — `voi.id/berita/587385/…`
- **hops.id** (42) — `hops.id/viral/29417443642/…`

All three currently pass PageRank (your profile is 149 followed / 1 nofollowed).

- [?] Were any of these paid? → `________________`
- [ ] If yes → ask the publisher to add `rel="sponsored"`

**Why:** paid links that pass PageRank violate Google's policy. These are also your three most valuable links — worth protecting properly rather than risking.

---

### 8. [?] DECIDE: the "560+ / Michelin-trained" claims (open since 2026-07-28)

Live right now: `560+` in **exactly 39 files** under `src/`; `Michelin-trained` in **exactly 12 meta descriptions** including the homepage. Also live on `/private-chef-bali`: *"12,000+ guests"*, *"500+ villa bookings"*, *"50+ professional chefs"*.

- [?] Can you produce a booking count supporting "560+ events" and "12,000+ guests"? → `________________`
- [?] Who exactly is Michelin-trained? Adriano's own live bio says *"Fine-dining trained in Milan"* — a different claim → `________________`

Then:
- [ ] **Substantiate** — write the evidence into `seo-department/operations/` and keep the claims (they're strong if true)
- [ ] **Soften** — edit `src/data/siteFacts.ts:45,48,49,50` plus the 12 meta descriptions. Suggested: *"hundreds of villa events across Bali"*, *"a Milan-trained executive chef"*

**Do "Michelin-trained" first.** Applying it to the whole team across 12 meta descriptions when the chef's own bio says something narrower is the weakest, most challengeable claim on the site.

---

### 9. [?] DECIDE: resolve the three-way "private chef bali" collision

All three of these are in the live sitemap competing for your biggest commercial query (749 impressions, position 18.4):

| URL | GSC to 2026-07-26 | What it is now |
|---|---|---|
| `/private-chef-bali` | no data (live only since ~2026-08-02) | The intended pillar. 196 internal references |
| `/fine-dining/private-chef-bali` | 161 imp, **position 65** | Retitled *"Michelin Tasting Menu Bali"* — URL says private chef, page sells a tasting menu |
| `/guide/private-chef-bali` | 5 imp, position 42 | Guide hub |

- [ ] Keep `/private-chef-bali` as the commercial pillar — it is correct and correctly linked
- [ ] Add a prominent internal link from `/fine-dining/private-chef-bali` → `/private-chef-bali` (do this regardless of what else you decide)
- [?] For `/fine-dining/private-chef-bali`, choose: → `________________`
  - [ ] Rename the URL to match its tasting-menu content, with a 301
  - [ ] Point its canonical at `/private-chef-bali`
  - [ ] Leave it and rely on the internal link
- [ ] **Do NOT delete it.** It holds 161 impressions and inbound links, including the hops.id link. Any change needs a redirect plan (department rule)

---

### 10. Re-run the Semrush crawl

- [ ] Trigger a fresh Site Audit on campaign **30621470**
- [ ] Current snapshot is **2026-07-29** — it predates your 2026-07-30 schema fix and the 2026-08-03 B2B launch
- [ ] It still lists 3 structured-data errors on `/wedding-catering-indonesia`, `/proposal-dinner`, `/experiences/kids-birthday-chef-party` — **these are almost certainly already fixed** (`ProposalDinnerPage.tsx:232` now uses a valid `Organization` provider). Verify against the new crawl before touching any code
- [?] The crawl covers 100 of ~240 URLs. Raise the page limit if your plan allows → `________________`

---

### 11. Fix the internal links pointing at the homepage

- [ ] **`Footer.tsx:113–117`** — "Tasting Menu at Your Villa" → currently a hardcoded `/` override when on the pillar page. Change to **`/fine-dining/private-chef-bali`**. (Not `/fine-dining/tasting-menu` — the footer already links there at `:163`)
- [ ] **`Footer.tsx:459`** — "Reviews" → currently `/`. A real **`/reviews`** page exists (`page-meta.ts:2078`, in the live sitemap). Point at it
- [ ] **Footer** — "Private Chef Breakfast" → currently `/catering`. Change to `/catering/floating-breakfast` or remove
- [ ] Note: the "About" nav link lives in **`src/data/siteArchitecture.ts:501`** (`PRIMARY_NAV`), not Footer.tsx. It points to `/fine-dining/our-chefs`, which is reasonable — leave it

---

### 12. [?] Reconsider the bar-services footer links

- [ ] The footer links to `/bar-services/#hotels`, `#villas`, `#beach-clubs`, `#restaurants`, `#events` from **every page** — a 22-URL cluster
- [ ] But you deliberately removed all 22 from the sitemap (owner decision, 2026-07-28) to protect crawl budget for the area pages
- [?] Decide: reduce to a single footer link, or put the cluster back in the sitemap → `________________`

**Why:** right now you're excluding it with one hand and pushing crawl equity at it from ~240 pages with the other.

---

### 13. Trim the over-long meta descriptions

- [ ] **64** descriptions in `page-meta.ts` exceed 160 characters and will truncate in Google
- [ ] Don't do all 64. Do the ones that actually earn impressions: `/`, `/locations/canggu`, `/locations/ubud`, `/locations/seminyak`, `/catering`, `/catering/bbq-catering`, `/events/weddings`, `/blog/private-chef-cost-bali`

---

## MONTH 1 — content and rankings

### 14. Fix the wedding cluster — your largest unowned family

`bali wedding catering` = **424 impressions, position 27.9, 2 clicks**. Second-biggest query, and you're on page 3.

- [ ] `/events/weddings` — position 38.4, **373 impressions, zero clicks.** Review title, description, H1
- [ ] `/blog/wedding-private-chef-bali-planning-guide` — position **11.9**, 271 impressions, **zero clicks.** Position 12 at 0% CTR is a snippet problem. Rewrite title and description **first**, before anything structural
- [ ] Decide which of the two owns the commercial query, and internally link the other to it

---

### 15. Push the near-miss keywords (positions 10–20)

302 impressions sitting just off page one:

| Query | Imp | Pos | Owner page |
|---|---|---|---|
| `bali villa catering` | 94 | 15.4 | `/catering/villa-catering` |
| `private chef bali villa` | 94 | 15.9 | `/private-chef-bali` |
| `private chef villa menu` | 73 | 12.0 | `/fine-dining/menus` |
| `corporate event catering bali` | 41 | 18.5 | `/catering/corporate-catering` |

- [ ] For each: confirm the target page's H1, title and opening paragraph actually use the phrase
- [ ] Add internal links using that phrase as anchor text
- [ ] **Do not create new pages** for these — you'd cannibalise existing equity (exactly what D-012 avoided)

---

### 16. [?] DECIDE: is Jakarta a real business line?

Nearly hidden by the Bali framing, but the data is unambiguous:

- `/private-chef-senayan` — **47 clicks, position 5.78** (your second-best page after the homepage)
- `/jakarta` — 16 clicks, position 10.3
- `/private-chef-menteng` — 8 clicks, position 12.8
- Query `private chef jakarta` — **19 clicks, position 5.9**
- Indonesia overall: **8,670 impressions, 264 clicks** — bigger than the US and Australia combined

- [?] Do you actively want Jakarta business? → `________________`
- [ ] If yes → give it what Bali got: a pillar page, area pages, internal linking
- [ ] If no → leave it, it earns clicks for free

---

### 17. Do not chase the deep-position queries

- [ ] `catering bali` (pos 38), `best catering in bali` (pos 54), `catering chef` (pos 54), `private dining` (pos 25)
- [ ] **No on-page change moves position 54 to position 8.** These need domain authority
- [ ] Leave them until the link work in step 18 has run for a few months

---

## ONGOING — link building (the long pole)

### 18. Build real links, in this order

You are **DR 23 / Authority Score 6** with 69 referring domains, of which about 3 are genuinely valuable. That is why 290 queries sit below position 20. On-page work is close to its limit.

- [ ] **1. Villa management and villa rental companies in Bali** — relevant, and a referral channel as well as a link. Pitch with `/staffing/for-villa-managers`
- [ ] **2. Wedding planners and Bali venue directories** — unlocks the 424-impression wedding cluster
- [ ] **3. Expat / relocation publications** — you already have `/blog/private-chef-bali-expats`
- [ ] **4. More Indonesian media** — Tribun/VOI/Hops worked. Repeat whatever produced them. Label paid placements as sponsored (step 7)

**Do not:**
- [ ] ✗ Buy directory links
- [ ] ✗ Build more satellite sites
- [ ] ✗ Guest post on generic lifestyle blogs with no Bali relevance

- [ ] Log every new link in `seo-department/operations/LINK-LEDGER.csv`

---

### 19. Hold off on the disavow file

- [ ] ~50 of your 69 referring domains are automated "buy aged domains and backlinks" farms
- [ ] Almost certainly **not yours**, and Google generally ignores them
- [ ] Disavow is blunt and easy to over-apply. Only revisit if you get a manual action in GSC

---

## SCHEDULED CHECKS

### 20. 2026-08-26 — the consolidation verdict ⚠️

- [ ] Pull a fresh GSC export
- [ ] Compare against this baseline (28 Apr–26 Jul):
  - `/private-chef/` family: **2 pages with data, 2 clicks, 65 impressions**
  - `/locations/` family: **10 pages, 104 clicks, 3,606 impressions**
  - `private chef canggu` pos 5.9 · `private chef ubud` pos 6.5 · `private chef uluwatu` pos 6.3
- [ ] D-010/D-020 redirected traffic **from** the family earning 104 clicks **to** the family earning 2. The real observation window opened 2026-07-29
- [ ] If `/private-chef/` hasn't moved by then → reconsider the direction, with evidence, and log a new decision

---

### 21. Mid-September — did the pillar work?

- [ ] `/private-chef-bali` went live in the sitemap around **2026-08-02**. First meaningful GSC data lands ~6 weeks later
- [ ] Check: position of `private chef bali` (baseline **18.4**, 749 impressions, 21 clicks)
- [ ] Check: impressions for `/private-chef-bali` (baseline: no data — it postdates the export)
- [ ] Check: whether `/fine-dining/private-chef-bali` (baseline position 65) has stopped competing for hire intent

---

### 22. Update the risk register

- [ ] **Close R-006** — WhatsApp conversion tracking is live (101 `generate_lead` events since 2026-07-30)
- [ ] **Close R-007** — SPA render parity verified 2026-08-05; prerendered HTML has full content + 6 schema blocks
- [ ] **Add R-009** (satellite link scheme, CRITICAL), **R-010** (advertorial followed links, HIGH), **R-011** (homepage price contradiction, HIGH), **R-012** (stale generated artefacts in git, HIGH), **R-013** (three-way head-term collision, HIGH)
- [ ] **Update R-002** to "partially resolved, review 2026-08-26"

---

## ACCESS TO REQUEST

Blocking real analysis, not nice-to-haves:

- [ ] **GSC API access** — so audits stop depending on manual CSV exports that go stale in a week
- [ ] **Ahrefs paid plan** — the free tier gives Domain Rating and nothing else. Needed for competitor link-gap analysis, the input for step 18
- [ ] **Google Business Profile access** — for "private chef bali" the local map pack likely sits above the organic result you're chasing. Completely unmeasured
- [ ] **Semrush crawl limit** raised to ~240 URLs
- [ ] **Booking / revenue data** — 49 organic leads/month is measured; whether they convert, and at what value, is not

---

## SCORECARD

| | |
|---|---|
| Critical | 1 (C-1, link profile) |
| High | 4 |
| Medium | 6 |
| Low | 8 |
| Decisions needed from you | 7 |
| Week-1 effort | ~2 hours, all low-risk |
| Biggest single win | **Step 1** — 10 minutes |
| Highest-stakes item | **Step 6** — satellite sites |

---

### 23. [?] DECIDE: the HACCP certification claim (NEW — opened 2026-08-05)

`src/pages/FoodAllergiesPage.tsx:242` states:

> "HACCP (Hazard Analysis Critical Control Points) is the food safety standard used in commercial food production. **All myCHEF chefs are certified.** This is how allergen safety works across a full booking."

- [?] Do all myCHEF chefs currently hold valid HACCP certification? → `________________`
- [ ] If **yes** — record the evidence in `seo-department/operations/` and keep it. It is a genuine differentiator
- [ ] If **not universal** — reword to describe the procedure rather than assert a credential, e.g. *"We work to HACCP principles: separate prep surfaces, dedicated utensils, and allergen briefing before every service."*

**Why this one matters more than the others:** it is a food-safety credential on an allergy page. It sits in both R-003 (unsubstantiated claims) and R-004 (never guarantee allergy outcomes). Of everything left open, this is the one with actual guest-safety and liability exposure. Filed as **R-014**.

---

### 24. Residual "Michelin-trained" scope (from step 8)

The 12 meta descriptions are done. **139 occurrences remain across 47 files** of body copy, and two pages are *built on* the claim:

- [ ] `/michelin-private-chef-bali-prices` — title, H1 and whole premise. Currently 13 impressions at position 11.4
- [ ] `/journal/michelin-training-bali` — an article about Michelin training
- [?] Decide: rewrite these two around "Milan-trained / fine-dining trained", or keep them as a deliberate play for the "michelin" query family → `________________`
- [ ] Then sweep the remaining body copy. Top files: `articleContent.ts` (21), `LunaPage.tsx` (8), `TastingMenuPage.tsx` (7), `HowToHirePrivateChefPage.tsx` (7), `HoneymoonChefPage.tsx` (6), `landingPages.ts` (6)

I did not sweep these automatically — a blind find-and-replace across 47 files of prose produces awkward sentences and would have silently gutted two pages' positioning. It needs a pass with eyes on it.
